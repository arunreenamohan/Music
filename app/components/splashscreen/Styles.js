import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      },
      activityIndicator: {
        alignItems: 'center',
        height: 80,
      },
      textStyle: {
        textAlign: 'center',
        fontSize: 25,
        color: '#008080',
        fontWeight:'700'
      },
});

export {styles};